{
  "name": "displayname",
  "version": "0.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "test": "istanbul cover ./node_modules/tape/bin/tape ./tests/*.js && istanbul check-coverage --statements 100 --functions 100 --lines 100 --branches 100",
    "jshint": "jshint -c .jshintrc --exclude-path .gitignore lib tests",
    "codestyle": "jscs -p google lib/ tests/",
    "coverage": "open coverage/lcov-report/index.html"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "mustache": "^0.8.2"
  },
  "devDependencies": {
    "istanbul": "^0.3.2",
    "jshint": "^2.5.6",
    "jscs": "^1.6.2",
    "tape": "^3.0.0",
    "pre-commit": "0.0.9"
  }
}
